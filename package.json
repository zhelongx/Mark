{
  "name": "@zhelongx/mark",
  "version": "0.0.5",
  "productVersion": "0.05",
  "private": true,
  "description": "ZhelongX / Mark — a focused screen-annotation tool rack.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer/toolbar.js && node --check src/renderer/overlay.js"
  },
  "devDependencies": {
    "electron": "43.3.0"
  }
}
