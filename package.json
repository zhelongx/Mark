{
  "name": "@zhelongx/mark",
  "version": "0.0.6",
  "productVersion": "0.06",
  "private": true,
  "description": "ZhelongX / Mark — a focused screen-annotation tool rack.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer/toolbar.js && node --check src/renderer/overlay.js && node tools/r9-overlay-input-audit.mjs"
  },
  "devDependencies": {
    "electron": "43.3.0"
  }
}
